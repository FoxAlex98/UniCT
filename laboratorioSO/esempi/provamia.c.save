#include<stdio.h>
#include<stdlib.h>
#include<unistd.h>

int main(in-tt argc, char* argv[]){
  	pid
	if(argc<2)
	{
	 printf("metti un altro parametro oltre %s\n",argv[0]);
	 exit(1);
	}

	int pid=fork();

	if(pid==0){
	  printf("sono il figlio");
	}
	else{
	  printf("sono il padre");
	}
	
	printf("e faccio il mio lavoro");
}
